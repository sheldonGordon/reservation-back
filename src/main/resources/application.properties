spring.datasource.url= jdbc:postgresql://${db_hostname:127.0.0.1}:${db_port:5432}/${db_databaseName:reservation}
spring.datasource.username= ${db_username:reservation}
spring.datasource.password= ${db_userPassword:reservation}

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto= update